#!/usr/bin/env bash

export PROJECT_NAME=docker-symfony

# Path where your project is stored
export PROJECT_PATH=$HOME/www

# Path where docker is stored
export DOCKER_PATH=$HOME/docker

# Id of your user run "echo $(id -u)" in terminal to get it
export USER_ID=$(id -u)